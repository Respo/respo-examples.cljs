{
  "name": "respo-examples",
  "version": "0.1.0",
  "main": "index.js",
  "repository": "git@github.com:Respo/respo-examples.git",
  "author": "jiyinyiyong <jiyinyiyong@gmail.com>",
  "scripts": {
    "watch": "shadow-cljs watch app",
    "counter": "rm -f src && ln -s counter/src src",
    "counter-hot": "rm -f src && ln -s counter-hot/src src",
    "buttons": "rm -f src && ln -s buttons/src src",
    "shopping-cart": "rm -f src && ln -s shopping-cart/src src",
    "todomvc": "rm -f src && ln -s todomvc/src src",
    "hiccup": "rm -f src && ln -s hiccup/src src"
  },
  "license": "MIT",
  "devDependencies": {
    "shadow-cljs": "^2.0.92"
  }
}
